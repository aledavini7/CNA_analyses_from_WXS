{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/aledavini7/CNA_analyses_from_WXS/main/nextflow_schema.json",
  "title": "CNA analyses from WXS parameters",
  "description": "Schema for launching the WXS CNA analysis pipeline from Seqera.",
  "type": "object",
  "properties": {
    "bam_dir": {
      "type": "string",
      "format": "file-path",
      "x-label": "BAM directory",
      "description": "Directory containing sample folders with BAM files. All BAMs are discovered recursively.",
      "fa_icon": "fas fa-folder-open"
    },
    "outdir": {
      "type": "string",
      "default": "results",
      "description": "Output directory."
    }
  },
  "required": ["bam_dir"]
}
